import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Persistence roundtrip (Windows) - scaffold', () {
    testWidgets('save → close → reopen equals last state (placeholder)', (tester) async {
      expect(true, isTrue);
    }, skip: true);
  });
}








